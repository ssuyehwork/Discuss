# 0. 关于 ArcMeta 开发规则

## 0.1 职责单一原则
全应用设计必须死守单一职责原则（SRP）。任何服务层不应混入底层数据持久化的原生 API（如 SQL、C 语言 sqlite3 接口等）；核心文件遍历扫描不应深度耦合特定业务的元数据解析。

## 0.2 双轨模式隔离原则
应用分为"SQLite 内存模式"（托管分类模式）和"磁盘目录模式"两种。两者运行时各自独立，不共享判断逻辑、不共享数据加载路径。

---

# 1. 关于"文件过滤"

## 1.1
应用内的辅助配置文件、缩略图等辅助资产（如 `.ArcMeta.json`、`_thumbnail.png`、`metadata.scch`）应统一由集中式的文件过滤机制（如 `FileFilterService`）进行判定和隐藏，禁止在各个业务服务中分散编写同名的静态辅助过滤逻辑。

## 1.2
系统产生的 `.arc` 资产包文件夹在任何文件监控、自动对账扫描、目录遍历等生命周期运行时，必须在最前端进行强制拦截，使其始终隐形不可见，避免将其作为普通文件夹扫描并导入侧边栏分类树。

---

# 2. 关于"重命名编辑器与选中定位"

## 2.1
在编辑文件名进行重命名时，编辑器在触发、聚焦和展示的时序上必须完全自包含。严禁使用 `QTimer::singleShot` 异步延迟微调选中状态。一切选中定位操作、光标移动及事件捕获拦截必须由编辑器组件在同步事件流（如聚焦或显示事件）中自控完成。

## 2.2
编辑器的微调偏移量及尺寸几何逻辑必须动态自适应屏幕 DPI 与系统的字体度量（Font Metrics），严禁直接使用硬编码像素做强制坐标偏移，以确保在高分屏（DPI 125%、150% 等）下视觉比例完美、无遮挡。

---

# 3. 关于"数据访问与并发安全"

## 3.1
服务层不得直接调用和执行数据库底层连接（如获取原生 `sqlite3` 句柄进行查询或操作）。所有底层数据持久化查询与回收操作必须彻底下沉封装在持久层仓储类（如 `DiskTrashRepo`）内部。

## 3.2
禁止多个线程跨并发安全临界区直接并发争抢同一个底层数据库内存句柄。针对需要由后台线程、后台子任务访问的数据操作，必须在持久层内部建立严密的并发访问锁保护机制（如读写锁、互斥锁），以彻底避免 SQLite 锁竞争和由于并发句柄争夺引起的崩溃异常。
