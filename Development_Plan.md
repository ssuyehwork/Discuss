# 0. 关于 ArcMeta 开发规则

## 0.1 职责单一原则
全应用设计必须死守单一职责原则（SRP）。任何服务层不应混入底层数据持久化的原生 API（如 SQL、C 语言 sqlite3 接口等）；核心文件遍历扫描不应深度耦合特定业务的元数据解析。

## 0.2 双轨模式隔离原则
应用分为"SQLite 内存模式"（托管分类模式）和"磁盘目录模式"两种。两者运行时各自独立，不共享判断逻辑、不共享数据加载路径。

---

# 1. 关于"文件过滤"

## 1.1
应用内的辅助配置文件、缩略图、资产包等辅助资产与系统专用屏蔽路径（如 `.ArcMeta.json`、`_thumbnail.png`、`metadata.scch`、`.arc` 文件夹）应统一由集中式的文件过滤服务（如 `FileFilterService`）进行完全收拢判定 and 隐藏。

## 1.2
禁止任何业务服务、扫描服务或对账服务（如 `DiskScanService`）各自私下硬编码任何部分文件类型的过滤与屏蔽判定（包括 `.arc` 的过滤）。必须统一调用 `FileFilterService::isAuxiliaryFile` 或专门的判定，保证磁盘模式与资源库模式的文件识别标准绝对统一，不留缝缝补补的补丁。

---

# 2. 关于"重命名编辑器与选中定位"

## 2.1
在编辑文件名进行重命名时，编辑器在触发、聚焦 and 展示的时序上必须完全自包含。严禁使用 `QTimer::singleShot` 异步延迟微调选中状态。一切选中定位操作必须在数据填充完毕后，在同步流程中紧接着调用，以防时序颠倒导致计算空文本产生失效。

## 2.2
编辑器的微调偏移量及尺寸几何逻辑必须动态自适应屏幕 DPI 与系统的字体度量（Font Metrics），严禁直接使用硬编码像素做强制坐标偏移，以确保在高分屏（DPI 125%、150% 等）下视觉比例完美、无遮挡。

---

# 3. 关于"数据访问与并发安全"

## 3.1
服务层不得直接调用和执行数据库底层连接（如获取原生 `sqlite3` 句柄进行查询或操作）。所有底层数据持久化查询与回收操作必须彻底下沉封装在持久层仓储类（如 `DiskTrashRepo`）内部。

## 3.2
禁止在外部 Repository 或各个业务服务层私自搞一个自欺欺人的、局部隔离的静态锁（如 Local QMutex）。所有涉及数据库的并发与线程安全锁必须由 `DatabaseManager` 底层（或其暴露的线程安全加锁查询接口，例如 `DatabaseManager::instance().executeLockedSelect` 或直接利用其原生的 Drive/Global 锁）统一安全控制，彻底消除原生 SQLite 跨线程多线程并发读写崩溃隐患。

---

# 4. 关于"批量重命名"

## 4.1
批量重命名操作完成后，不应弹出任何模式阻碍性、会导致代码运行阻塞的模态对话框（如弹窗提示）。必须统一调用全局自定义 `ToolTipOverlay::instance()->showText` 进行优雅的、非阻塞性通知，从而彻底消除代码运行阻塞及事件流的中断。

---

# 5. 关于"QSS 与全局样式架构"

## 5.1 QSS 资产纯净化原则
全应用应维护单一且极简的 QSS 文件架构（即以 `resources/style.qss` 为核心样式资产）。任何废弃、未被加载、未被 RCC 资源加载系统引用的死代码 QSS 文件（如 `dark_style.qss`、`scan_dialog.qss`）必须物理清理彻底，避免在工程资产中制造不一致或冗余垃圾。

## 5.2 拒绝 C++ 冗余兜底样式
严禁在 C++ 逻辑中通过 C++ 字符串拼接、硬编码方式为整个窗口框架设计上百行的兜底样式表（即在 QStyleSheet 加载失败时做庞大的内联降级渲染）。主样式必须全部声明在 `style.qss` 统一层叠样式表中。若外部资源确不可得，仅允许进行最小限度的结构化色值覆盖，彻底贯彻 DRY（Don't Repeat Yourself）思想。

## 5.3 严格限制内联样式滥用
禁止在子控件上过度滥用内联的 `setStyleSheet`（如大段硬编码设置圆角、边框、字体、背景等）。所有全局性、结构性 UI 组件的高亮、选择状态、尺寸间距、滚动条样式必须统一下沉至 `style.qss` 中，使 C++ 业务代码逻辑保持极致清爽、高内聚和架构可扩展性。
