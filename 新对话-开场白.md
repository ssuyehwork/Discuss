为了在新对话中让新的 AI **100% 继承我们此前确立的全部架构成果、绝不推翻重来、绝不再次转圈圈**，您只需要将下面这份**《QuarkMeta 架构无缝交接指令包》**直接复制并发送给新对话的第一条消息：

---

# 📋 新对话第一条发送的指令模板（直接复制）

```markdown
# 任务：接管 QuarkMeta 项目架构重构与后续开发

## 🛑 第一部分：核心行动纪律（铁律约束）
1. **【严禁转圈与重复重构】**：下方清单中列为【已完成重构】的模块，其职责和架构已彻底固化，严禁推翻、严禁重新修改已有签名、严禁带用户反复转圈。
2. **【绝对零补丁原则】**：严禁使用裸 Win32 API 夺焦、严禁使用 CSS padding 硬推几何布局、严禁声明 friend class 友元、严禁在 UI 控件中写底层文件 I/O。
3. **【先探讨，未经允许严禁擅自生成实施方案】**：在面对任何新需求或 Bug 时，必须先进行事实与因果链分析，只有在用户明确下达“授权/生成实施方案”指令后，才允许输出方案代码。

---

## 🏛️ 第二部分：全系统五层架构基准（已确立的架构底座）
本工程严格遵循以下五层 Clean Architecture：
1. **外壳层**：`FramelessWindowHelper`（无边框拉伸/拖拽/置顶/防遮挡）、`DeviceWatcher`（原生硬件监听，主窗口 0 行 Win32 代码）。
2. **呈现层**：`MainWindow`（纯装配容器，<150行，0业务状态）、5 大子面板（NavPanel, FavoritePanel, ContentPanel, MetaPanel, FilterPanel）、`TaskProgressToolBar`、`QuickLookWindow`。
3. **协调层**：`PanelLayoutManager`（230px 分栏与显隐）、`PanelMediator`（纯信号路由，无 friend 侵入，无 Model 穿透）、`AppShortcutController`（基于 QShortcut(Qt::WindowShortcut) 局内绑定）、`TrayController`（标准托盘托管，无 exit(0)）。
4. **领域层 (SSOT 唯一真理源)**：
   - `NavigationService`（路径与双向历史栈唯一持有者）
   - `TrashService`（可逆软删除、原路冲突还原、7 秒撤销快照）
   - `PermanentDeleteService`（不可逆高危确认、物理粉碎、清洗撤销栈）
   - `ClipboardService`（复制/剪切/canPaste 判定/截图直粘/文件传输）
   - `ProtectionService`（.amenc 8字节魔数头、1ms 验密、物理还原与 RAII 预览）
   - `TagLexiconService`（纯 SQLite global.db 词库字典、前缀联想，绝无全盘扫描）
   - `BatchRenameService`（规则解析、冲突仲裁、两阶段 UUID 安全中转、单向快照）
   - `TaskProgressService`（线程安全任务队列调度与多任务加权进度计算）
   - `AppLifecycleManager`（四阶段受控优雅退出清场、SQLite WAL Checkpoint 合流）
5. **基础设施层**：
   - `ColorPaletteEngine`（纯底层图像色彩算法，位于 src/util/）
   - `ThumbnailPipelineService`（三级缓存：内存 LRU -> 磁盘 Hash -> 后台无锁 QImage 解码 + 代际熔断）
   - `FilterProxyModel`（独立于 ContentPanel 的复合过滤代理模型）
   - `DiskIoService`、`DatabaseManager`、`OperationSnapshotEngine`、`UndoManager`。

---

## 🗑️ 第三部分：已被彻底物理删除的废弃类（严禁重新引入！）
以下 8 个类已被彻底删除并注销：`ResizeEventFilter`、`TitleBarEventFilter`、`DiskBatchRenameService`、`GlobalShortcutController`、`TaskProgressController`、`MediaColorExtractor`、`ColorAlgorithmEngine`。

---

## 📌 第四部分：当前项目所处的最新状态
目前系统的 19 大核心领域架构已经全部完成方案交付。
请直接向我汇报：“我已完全掌握 QuarkMeta 的五层架构基准与 19 大领域服务状态，严禁擅自修改已完成模块，请提供您当前遇到的具体编译报错或需要探讨的具体业务。”
```

---

### 为什么这份指令能防止新 AI“转圈”和“破坏代码”？

1. **封死了 AI“重新发明轮子”的冲动**：清单里明确列出了 19 个已经完成的模块，新 AI 一进来就知道这些是“不可动摇的既定事实”，不会再带您重新讨论一遍；
2. **锁死了已删除的 8 个垃圾类**：防止新 AI 犯傻又把 `ResizeEventFilter` 或 `GlobalShortcutController` 重新写回来；
3. **建立了“先探讨、严禁直接甩方案”的强制流程控制**。