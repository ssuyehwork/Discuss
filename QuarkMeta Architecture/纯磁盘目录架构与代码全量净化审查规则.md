已将**“全量根除旧架构判断分流与冗余分支（包含相关注释）”**深度整合进提示词中。以下是最终完整版提示词：

---

```markdown
# 纯磁盘目录架构与代码全量净化审查规则

你必须无条件严格遵循以下架构红线与代码审查准则：

## 一、 核心架构基准（纯磁盘目录模式）
本系统已彻底剥离旧版“受控库/内存模式”，当前为纯粹的【纯磁盘目录模式】：
1. **单文件/单目录元数据**（星级、颜色、标签、尺寸、宽高比、备注、链接等）：100% 仅读写物理文件夹内部隐藏的 `.QuarkMeta.json`。
2. **全局数据库 `global.db`**：仅且只能维护三张表：标签组（tag_groups / tag_group_items）、磁盘物理回收站（disk_trash）与盘符属性（DriveMeta）。
3. **严禁在 `global.db` 中读写或创建 `metadata` 表**。

## 二、 僵尸代码与废弃体系彻底根除红线（零容忍）
任何出现以下旧体系的代码，均视为严重残留，必须从根本上彻底删除，严禁采用打补丁或保留空实现的兼容方式：
1. **旧架构判断分流与冗余分支（重点根除）**：
   - 严禁出现任何基于旧模式的分流判断代码，如 `if (type == "category")`、`if (scopeSource == "category")`、`if (dataSourceType == UserCategory)`、`if (isDiskMode) ... else ...`、`if (isInsideArc)` 等。
   - 纯磁盘模式下**只有一套统一逻辑**，严禁保留针对旧分类、旧受控库或多数据源的假分流与防御性兜底分支。
2. **`.arc` 胶囊容器体系**：严禁出现 `.arc` 穿透、解包、截断、过滤等任何逻辑。
3. **虚拟 ID 与旧分类体系**：严禁出现 `folderId`、`FID`、`FRN`、`m_folderIdToPath` 及查分类的倒排索引（`m_assetNameToFolderIds`、`m_subFolderNameToFolderIds`、`m_extensionToFolderIds` 等）。
4. **入库进度体系**：严禁出现 `ingestionStatus`、`registrationProgress`、`calculateAndPersistProgress` 及文件夹进度百分比计算逻辑。
5. **双轨回收站体系**：严禁出现双轨分组隔离、`IsGroupHeaderRole`、`GroupNameRole`。
6. **空桩函数与幽灵变量**：严禁保留任何 `{ return false; }` 或 `{}` 的空实现兼容桩（如 `hasPendingSync`、`registerQuarkMetaFrn`、`loadVolumeNameCache` 等）。

## 三、 注释与分流标记全量净化红线
清退代码的同时，**必须连同所有相关注释与分流标记一并彻底根除**：
1. 严禁残留任何分流说明注释（如 `// --- 分流 A：物理磁盘导航模式 ---`、`// --- 分流 B：受控模式 ---`、`// 分类联动逻辑` 等）。
2. 严禁残留任何历史计划与补丁编号注释（如 `Plan-XX`、`[Plan-128]`、`[1.1.4 规范]`、`Development_Plan`）。
3. 严禁残留已废弃概念的说明注释（如“受控模式”、“胶囊入库”、“虚拟树索引”、“双轨隔离”等）。
4. 严禁残留任何修饰性、口语化的历史维护标记（如 `// 🚨 核心修复...`、`// 2026-xx-xx 按照要求...`）。
5. 代码注释必须保持专业、纯粹，仅准确描述当前纯磁盘架构的实际逻辑。

## 四、 审查与交互规范
1. **杜绝恭维与废话**：直奔核心，重结果，轻冗余技术解释。
2. **禁止未经明确指令擅自输出大段重构代码**：排查时仅精准列出问题所在文件、具体代码行与根治方案。
3. **全局审视与零猜测**：必须基于实际源码做深度排查，杜绝局部打补丁引发的假死与编译连锁报错。
```