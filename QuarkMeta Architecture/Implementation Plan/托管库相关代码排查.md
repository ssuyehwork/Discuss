深度排查分析报告已整理完毕，具体排查结果如下：

一、 用户指定的 18 个文件排查与清理方案
沙盒磁盘物理存留状态：

已物理移除的文件（6个）：
src/util/CapsuleMediaExtractor.h / CapsuleMediaExtractor.cpp
src/meta/CategoryRepo.h / CategoryRepo.cpp
src/core/CategoryLoadService.h / CategoryLoadService.cpp
仍残存在磁盘上的物理文件（本次将彻底物理删除）（12个）：
src/util/AssetImporter.h / AssetImporter.cpp
src/util/ImportHelper.h / ImportHelper.cpp
src/ui/CategoryLockDialog.h / CategoryLockDialog.cpp
src/ui/CategoryLockWidget.h / CategoryLockWidget.cpp
src/ui/CategoryPanel.h / CategoryPanel.cpp
src/meta/AmMetaJson.h / AmMetaJson.cpp
核心源文件中的幽灵引用与悬空逻辑：

src/main.cpp：清理 #include "ui/CategoryPanel.h"、CategoryLockDialog.h、CategoryRepo.h。
src/ui/MainWindow.h / MainWindow.cpp：清理 #include "CategoryPanel.h"、ImportHelper.h、AssetImporter.h、CategoryRepo.h、SyncStatusService.h，删除 m_categoryPanel 成员定义、初始化及关联布局，移除 ImportHelper 调用。
src/ui/ContentPanel.h / ContentPanel.cpp：清理 #include "CategoryLockDialog.h"、CategoryLockWidget.h，删除前置声明与成员变量 m_lockWidget，移除锁控件实例化与面板关联。
src/ui/MetaPanel.cpp：清理 #include "CategoryPanel.h"。
src/ui/BatchRenameDialog.cpp：清理 #include "../meta/CategoryRepo.h" 及 CategoryRepo::renamePhysicalCategoryPath 调用。
src/core/CoreController.cpp：清理 ArcMeta::CategoryRepo::initialize()。
src/core/BasicCommands.h：清理 CategoryRepo 关联的分类命令代码。
src/core/AutoImportManager.cpp：清理 CategoryRepo::syncPhysicalDirectoryCascade 调用。
src/meta/MetadataManager.cpp：清理 CategoryRepo 静态调用。
src/util/ShellHelper.cpp：清理 CategoryRepo::removeAllCategories 调用。
二、 衍生配套僵尸文件与内存模式残留逻辑
衍生孤立死代码物理删除：

src/ui/CategoryModel.h / CategoryModel.cpp
src/ui/CategoryFilterProxyModel.h
src/ui/CategoryDelegate.h / CategoryDelegate.cpp
src/meta/CategoryBindingManager.h / CategoryBindingManager.cpp
src/core/SyncStatusService.h / SyncStatusService.cpp
内存模式（Memory Mode）双轨分支清除：

src/ui/BatchCreateDialog.h / BatchCreateDialog.cpp：彻底移除 isMemoryMode 参数、m_isMemoryMode 成员及 m_libraryCombo 选择下拉框分支逻辑，简化为纯磁盘模式。
CMakeLists.txt 构建清理：

彻底从 CMakeLists.txt 的 SOURCES 列表中删除 SyncStatusService.cpp 与 SyncStatusService.h。
请审阅上述排查报告！如果认可排查结果，请批准我编写无脑实施方案 memory_mode_purge.md（保存在 QuarkMeta Architecture/Implementation Plan/ 目录下）。