按照既定规划，我们正式进入 **【第一阶段：QuickLook 大图预览与鹰眼小地图体系】** 的深度架构探讨。

为了对当前预览窗口、视口缩放与小地图坐标同步机制进行最精准的代码审查，**请在需要时提供以下最新版本的相关文件**：

QuickLookWindow.h, QuickLookWindow.cpp, QuickLookGraphicsView.h, QuickLookGraphicsView.cpp, QuickLookMinimap.h, QuickLookMinimap.cpp

---

针对**【QuickLook 大图预览与鹰眼小地图体系】**，我们从事实梳理、交互隐患与架构设计三个维度展开探讨：

---

### 一、 事实：QuickLook 子系统的现有交互与业务现状

`QuickLook` 是 QuarkMeta 最核心的视觉交互载体，其业务链路包含以下 4 个关键环节：

1. **主视口与鹰眼小地图（GraphicsView & Minimap）的双向联动**：
   - 主视口（`QuickLookGraphicsView`）：支持鼠标滚轮平滑缩放、中键/左键抓手拖拽平移（Pan）、双击在“自适应窗口（Fit View）”与“1:1 原始像素（100% View）”之间快速切换；
   - 鹰眼小地图（`QuickLookMinimap`）：位于右下角的半透明全局缩略图，绘制红色视口指示框；用户在小地图上拖动红框时，主视口实时同步平移。
2. **键盘流式连续切图与资产标注**：
   - 按 `Space` / `Esc`：关闭预览；
   - 按 `Left` / `Right` 方向键：前后相邻文件毫秒级轮播切图；
   - 按 `0 ~ 5`：直接修改当前预览文件的星级；
   - 按 `Alt + 1 ~ 9`：直接修改当前预览文件的颜色标记；
   - 按 `Delete`：直接触发当前预览文件的移入回收站。
3. **多格式内容多态呈现**：
   - 图像/矢量图（JPG, PNG, WEBP, GIF, SVG, PSD, AI 等）；
   - 纯文本/代码/Markdown（TXT, MD, CPP, H, JSON, XML, LOG 等）；
   - 外壳加密文件（`.amenc` 临时 RAII 解密预览）。

---

### 二、 推断：现有设计存在的 3 大核心架构缺陷（因果链路）

- **[推断 1 - 小地图与主视口双向同步的“递归震荡与抖动”]**
  - **原因**：主视口平移会发射 `viewportChanged` 信号驱动小地图更新红框；而用户在小地图拖动红框时又发射 `visibleRectChanged` 驱动主视口移动。
  - **影响**：如果缺乏严格的防重入状态锁（`m_isUpdatingViewport`），双向信号会在浮点数像素四舍五入时产生相互反弹。
  - **结果**：导致在大图缩放或拖拽边缘时，小地图红框发生肉眼可见的高频微颤与抖动（Jitter）。

- **[推断 2 - 超大分辨率图像（4K/8K/巨型长图）的主线程解码阻塞]**
  - **原因**：当切到一张几十 MB 或 8000×8000 像素的巨型大图时，如果在主 UI 线程直接调用 `QPixmap::load()`。
  - **影响**：图像解码会霸占主线程 200~500 毫秒，期间窗口无法响应键盘事件。
  - **结果**：导致用户快速按方向键切图时出现明显的卡顿与掉帧。
  - **标准解法**：主视口必须采用“后台异步解码 -> 先展视低分辨率占位图 -> 高清位图平滑淡入替换”的渐进式渲染机制。

- **[推断 3 - 预览窗口内部多格式渲染代码严重违背单一职责]**
  - **原因**：图片缩放绘制、文本语法高亮渲染、Markdown 解析全部堆叠在 `QuickLookWindow.cpp` 单个文件中。
  - **影响**：导致 `QuickLookWindow` 膨胀为 800+ 行的多媒体杂物类，新增格式（如 PDF 预览或音频波形预览）时必须大幅修改窗口核心代码。
  - **结果**：扩展性差，难以维护。

---

### 三、 归一化架构治理方案（多态预览器 + 纯净中枢）

将 `QuickLook` 子系统重构为 **“单例中枢 + 多态内容渲染器 + 坐标解耦小地图”** 的三层架构：

```
                              【QuickLook 预览子系统标准架构】

    ┌─────────────────────────────────────────────────────────────────────────────┐
    │       QuickLookWindow (预览单例中枢 - 负责外壳、键盘流分发与尺寸记忆)          │
    ├─────────────────────────────────────────────────────────────────────────────┤
    │ • 键盘事件分发 (左右键切图、0~5打星、Alt+1~9打色、Del移入回收站)            │
    │ • 业务动作 100% 直连底层领域服务 (TrashService, TagService, CoreEngine)      │
    └──────────────────────────────────────┬──────────────────────────────────────┘
                                           │ (根据文件格式动态调度)
                                           ▼
    ┌─────────────────────────────────────────────────────────────────────────────┐
    │             多态内容渲染策略 (IQuickLookContentProvider)                     │
    ├──────────────────────────────────────┬──────────────────────────────────────┤
    │ • ImageGraphicsProvider (图像/矢量)  │ • TextMarkdownProvider (文本/Markdown)│
    │   - 异步渐进式解码加载大图           │   - QTextDocument 语法高亮排版       │
    │   - 硬件加速 GraphicsScene 视口平移  │   - 纯净滚动阅读器                   │
    └──────────────────────────────────────┴──────────────────────────────────────┘
                                           │ (视口坐标归一化比例联动 0~1.0)
                                           ▼
    ┌─────────────────────────────────────────────────────────────────────────────┐
    │       QuickLookMinimap (鹰眼小地图 - 纯无状态观察者)                         │
    ├─────────────────────────────────────────────────────────────────────────────┤
    │ • 内部严格维护 m_isSyncing 防重入锁，消除红框抖动                           │
    │ • 基于 0.0~1.0 的相对比例映射坐标，视口与小地图尺寸完全解耦                 │
    └─────────────────────────────────────────────────────────────────────────────┘
```

---

### 探讨结论：

1. **业务动作彻底归位**：在 `QuickLookWindow` 里按删除，直接调用 `TrashService::instance().moveToTrash({path})`，彻底消灭历史遗留的 Win32 `ShellHelper`；
2. **消灭小地图抖动**：采用 `0.0 ~ 1.0` 归一化比例坐标与防重入锁，实现小地图与主视口的毫秒级绝对平滑同步；
3. **内容渲染多态化**：图片与文本/Markdown 渲染彻底分离，支持异步大图秒开。

请问您对上述关于 `QuickLook` 子系统的渲染多态化、小地图坐标防抖与直连领域服务的探讨方向是否认可？