# 逻辑归一化执行提示词

> 用途：当项目里出现"同一类行为被多个地方各自独立实现一遍"的情况时，用这份提示词指导排查与重构，
> 而不是继续在某一个点上打补丁。以 `FramelessWindowHelper` 的 `WindowRole` 化重构为参照范本。

---

## 一、什么是"归一化"，什么不是

**归一化 = 找到同一类行为的多份独立实现，抽出一份带角色/类型参数的统一实现，让所有调用方共用。**

判断"是不是归一化对象"的标准，问自己三个问题：

1. 这几处代码是不是在做**同一件概念上的事**（比如"无边框窗口的拖拽/缩放"、"列表项的显隐持久化"、"弹窗的关闭清理"），只是各自的细节参数不同？
2. 这几处实现之间有没有出现过"改一处、另一处忘了改，导致行为不一致"的真实 bug？（这是最强的信号——本次会话里 `TagManagerDialog` 单独维护一套拖拽实现、跟 `MainWindow`/`FramelessWindowHelper` 长期不同步，就是典型案例）
3. 如果现在再新增一个同类场景（比如再加一种新窗口类型、再加一种新面板），是不是又要**照抄一份现有实现**才能用？如果是，说明现在的做法本身就是在鼓励复制粘贴。

三条里只要中两条以上，就是归一化的候选对象。

**不是归一化对象的情况（不要滥用这份提示词去乱改）：**
- 两处代码看起来像，但其实是两个不同领域的概念，只是恰好用了相似的写法（比如两个毫不相关模块都用了 `if (a) {...} else {...}` 这种通用结构，不代表它们该被合并）。
- 只出现过一次的实现，没有第二份可比对象——归一化需要至少两个真实存在的"平行实现"才有意义，不要为了"预防以后可能出现重复"而提前抽象。

---

## 二、执行顺序（严格按步骤来，禁止跳步）

### 第 1 步：全项目普查，列出所有相关实现点

在动任何代码之前，先把"这类行为"当前在项目里所有的实现位置**逐一列出**，包括：
- 具体文件名 + 函数/方法名
- 每一处实现目前的完整行为细节（哪怕看起来是"理所当然"的小细节，也要记下来——归一化最容易在这些细节上悄悄丢失行为）
- 每一处的调用方是谁

这一步产出一份**清单**，不产出任何代码改动。清单里少列一个实现点，后面的统一设计就会有盲区。

### 第 2 步：找出"角色/类型"这个隐藏维度

普查完之后，问：这些实现之间的差异，是不是能用**一个有限的、可枚举的"角色"或"类型"参数**来描述？

- 能枚举 → 设计一个 `enum class` 或等价的类型标识，作为归一化实现的输入参数（参照 `WindowRole::Primary/Tool/Dialog` 的做法）。
- 不能枚举，差异是无限连续的（比如每个调用方都要一个完全不同的自由参数）→ 说明这不是"角色差异"，是"参数差异"，归一化的产出应该是一个带参数的通用函数，而不是角色枚举，不要强行套枚举。

### 第 3 步：把每个实现点当前的行为，逐条填进"能力对照表"

为每一种角色/类型，列一张表，每一行是一项具体能力/行为，每一列是一个角色，格子里填"有/无"或具体参数值。

这张表**必须来自第 1 步的普查结果，不能凭印象或"应该是这样吧"去猜**——每一格都要能指回第 1 步里某一处具体代码。

### 第 4 步：先出文字方案，不写代码，等待确认

把第 1-3 步的结果整理成一份简短方案，包含：
- 统一后的接口/签名长什么样
- 能力对照表
- **明确标注每一处"行为变化"**——归一化的过程中，只要某个调用方原来的行为和统一后的表现有哪怕一点点不一样（即使你认为新行为更合理），都必须在方案里用醒目的方式单独列出来，不能默认合并掉。
- 改动会涉及哪些文件

**方案写完之后停下来，等待明确批准，禁止自行开始改代码。** 这条是硬性规定，不因为"看起来很显然该这么做"而破例。

### 第 5 步：批准后再动代码，且改动范围只能是方案里列出的文件

- 只改方案里明确列出的文件，不得顺手"顺便"修改方案之外看到的其他问题（哪怕那个问题看起来也很显眼）。发现方案外的问题，记下来，改完这次的范围之后单独提出，不要混在一次改动里。
- 改完之后，逐条对照第 4 步里的"能力对照表"和"行为变化清单"自查一遍，确认没有漏改、没有多改。

---

## 三、常见反面模式（发现自己在这么做，立刻停下）

- **"我觉得这两处代码差不多，直接抽个函数就行"** —— 跳过了第 1 步的完整普查，抽出来的函数大概率会漏掉某个调用方独有的细节行为。
- **"归一化的时候顺便把这个我看不顺眼的地方也改了"** —— 范围蔓延，一旦出问题无法定位是归一化本身的问题还是顺手改动引入的问题。
- **"这处行为跟其他地方不一样，但应该是笔误，我直接按统一后的来"** —— 任何"不一样"都必须先在方案里提出来问，不能自己判断是不是笔误。历史上的不一致，可能是刻意为之（比如某个对话框故意允许内容区拖拽），也可能真的是遗留 bug，这个判断权不在执行者手里。
- **"先改代码，改完再补一份说明"** —— 方案必须在改代码之前，不是事后补文档。

---

## 四、验收标准

一次归一化重构做得合不合格，看这三点：

1. 统一之后，新增一个同类场景（新角色/新调用方）时，理论上只需要"加一个枚举值 + 补一行能力表"，不需要再复制一份实现。
2. 所有原来分散的实现代码被真正删除了，不是"新写一份统一实现，旧的几份留着没删、只是没人调用了"——留着的死代码本身就是下一次归一化对象。
3. 每一处已知的行为变化，使用方（用户）都已经明确知情并确认接受，没有静默变更。
